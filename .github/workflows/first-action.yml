name: First Workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: 
    steps: 
      - name: Print greeting
        run: echo: "Hello world"
      - name: Print goodbye
        run: echo: "Goodbye!"
